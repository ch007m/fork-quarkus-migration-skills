name: spring-rest-api
description: Minimal Spring Boot REST API with validation (no database)
type: spring-boot
skill: spring-boot-to-quarkus
source: local  # source/ directory in this project
timeout: 300

checks:
  - builds
  - tests-pass
  - no-spring-deps
  - has-quarkus
  - starts-up
